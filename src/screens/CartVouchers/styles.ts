import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 16,
    backgroundColor: '#F6F8FC',
  },
  activeVouchers: {
    fontSize: 26,
    fontWeight: 'bold',
    marginBottom: 16,
  },
});
