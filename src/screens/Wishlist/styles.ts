import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingBottom: 20,
    backgroundColor: '#FFFFFF',
  },
  innerContainer: {
    flex: 1,
    padding: 16,
    backgroundColor: '#FFFFFF',
  },
  arrowButton: {
    marginLeft: 'auto',
    bottom: 10,
  },
});
